// TODO: might need to allow scrolling pre
// TODO: auto collapse console and un-collapse on button
// TODO: store module functions in a json and use a loop to present them
// TODO: reduce the code padding so they are not touching when directly on top of each other
import { createApp } from "vue";
import App from "./App.vue";

import "@picocss/pico/css/pico.css";
import "./assets/main.css";

createApp(App).mount("#app");
